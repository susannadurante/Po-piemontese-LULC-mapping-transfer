# Outputs

Example outputs of the pipeline (small files only).

Intended content:
- accuracy tables (confusion matrices, per-class metrics)
- separability and predictor-selection summaries
- figures

Raster and vector data (`.tif`, `.gpkg`, `.rds`) are excluded by `.gitignore`.

_Placeholder: files to be added._
